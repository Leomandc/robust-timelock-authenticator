[project]
name = "robust-timelock-authenticator"
authors = ["Security Team"]
description = "A decentralized timelock authentication mechanism for secure access control"
telemetry = false
requirements = []
[contracts.robust-timelock]
path = "contracts/robust-timelock.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
